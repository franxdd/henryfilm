import { DETAIL } from "../Actions/Actions.js";
const initialState = {
  allmovies: [],
  detail: {},
};
const rootRouter = (state = initialState, action) => {
  switch (key) {
    case value:
      break;

    default:
      break;
  }
};
