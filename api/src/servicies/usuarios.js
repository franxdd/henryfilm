const axios = require("axios");
require("dotenv").config();
const { API_KEY } = process.env;

const postUser = async (req, res) => {
  
 try {
    console.log('entro')




 } catch (error) {
    
 }
};


module.exports = {
    postUser
};
